<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BudgetApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan|adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"
                    android:windowSoftInputMode="adjustPan|adjustResize"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />

        </activity>
        <activity android:name=".page2"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".Signup"/>
        <activity android:name=".categorizes"/>
        <activity android:name=".addcustomcategory"/>
        <activity android:name=".firstpage"/>
        <activity
            android:name=".addtransamountandnote"
            android:windowSoftInputMode="adjustNothing"
            android:parentActivityName=".firstpage"
            />

        <activity
            android:name=".totalchart"
            android:windowSoftInputMode="adjustNothing"
            android:parentActivityName=".firstpage"
            />

        <activity
            android:name=".savingchart"
            android:windowSoftInputMode="adjustNothing"
            android:parentActivityName=".firstpage"
            />


        
    </application>

</manifest>